services:
  - type: web
    name: landcraft
    env: python
    buildCommand: |
      apt-get update && apt-get install -y python3-setuptools python3-wheel
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn app:app
